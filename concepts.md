
3 types of application you can build with nest js 

http server using the nest factory create 

microservices applications using nest factory createMicroservice

a standalone application using nestfactroy.createapplicationcontext 

types of modules ( Building blocks of nest applcaiton )

a module is a class which is anotated with a @Module({}) decorator

what are decorators 

decorators are special kind of function which can be attached to methods classes functions and it modifies there behivaour it can even add metadata 

what are controllers 

are anotated with the @controller() decorator

providers 

